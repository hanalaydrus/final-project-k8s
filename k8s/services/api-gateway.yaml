apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  labels:
    name: api-gateway
spec:
  ports:
    - port: 8080
  selector:
    name: api-gateway
---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: api-gateway
  labels:
    name: api-gateway
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: api-gateway
    spec:
      containers:
      - name: api-gateway
        image: gcr.io/tugas-akhir-hana/github-hanalaydrus-api-gateway:86f109479ef62972d84e437b10b86bf926afe82d
        
      restartPolicy: Always